# Boggle Wiz

## Run
Run the following from the `./boggle` directory
```
python main.py
```

## Testing
Run the following from the `./boggle` directory
```
python -m unittest tests.board tests.round tests.trie
```
